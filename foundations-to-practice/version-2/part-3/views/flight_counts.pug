html
  head
    title Version 2 Challenge Part 3
    link(rel='stylesheet' href='/flight_counts.css')
  body
    h1 Flight Counts
    h2 For passengers with #{minFlightCount} or more flights
    div(id='message')
      p= message
    table(class='flight-counts')
      tr
        th Passenger Name
        th Flight Count
      each passenger in flightCounts
        tr
          td= passenger.name
          td(class='flight-count')= passenger.flightcount
    div(class='total-flights')
      button(id='total-flights-button') Get total flights
      span(id='flight-count-total')
